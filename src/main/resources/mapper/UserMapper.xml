<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.amorauth.mapper.UserMapper">

    <resultMap id="UserResultMap" type="org.example.amorauth.entity.User">
        <id column="id" property="id"/>
        <result column="google_id" property="googleId"/>
        <result column="email" property="email"/>
        <result column="name" property="name"/>
        <result column="picture" property="picture"/>
        <result column="locale" property="locale"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByGoogleId" resultMap="UserResultMap">
        SELECT * FROM users WHERE google_id = #{googleId}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (google_id, email, name, picture, locale, created_at, updated_at)
        VALUES (#{googleId}, #{email}, #{name}, #{picture}, #{locale}, NOW(), NOW())
    </insert>

    <update id="updateUser">
        UPDATE users
        SET name = #{name},
            picture = #{picture},
            locale = #{locale},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
